interface SwitchRoutes {
  home: string;
  accounts: string;
  cards: string;
  transactions: string;
  balance: string;
  login: string;
  register: string;
  dashboard: string;
  root: string;
}

export const switchRoutes: SwitchRoutes = {
  home: "/home",
  accounts: "/accounts",
  cards: "/cards",
  transactions: "/transactions",
  balance: "/balance",
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  root: "/",
};
